%미분 전 함수와 미분 후 함수 정의
f = @(x)exp(-x)-x;
df = @(x)(-1)*exp(-x)-1;

% 초기 추정값 x_0.
x_0 = 1;

% 뉴턴 메소드를 적용하여 근을 근사합니다.
for i = 1:10
    x1 = x_0 - f(x_0) / df(x_0);
    fprintf('%d = %.6f\n', i, x1);
    x_0 = x1;
end